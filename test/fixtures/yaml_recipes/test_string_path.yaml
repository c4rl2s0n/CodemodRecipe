dslVersion: 1
id: test_string_path
name: test_string_path
description: Tests string format path with method navigation

args:
  - name: file
    required: true
    inputKind: file

steps:
  - edit:
      path: "{{file}}"
      steps:
        - insert:
            at: class:Settings > method:update @ stmt:last
            text: |
              print('string path test');

postExecution:
  - run: dart format .
