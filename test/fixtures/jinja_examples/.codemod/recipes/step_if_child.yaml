id: jinja.step_if.child
name: step_if_child
description: Atomic create used by the step-if orchestrator fixture.

args:
  - name: className
    required: true

steps:
  - create:
      path: "lib/generated/{{ className | snake_case }}_extra.dart"
      template: |
        class {{ className | pascal_case }}Extra {}
