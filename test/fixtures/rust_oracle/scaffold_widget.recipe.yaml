id: scaffold_widget
args:
  - name: featurePath
    required: true
  - name: name
    required: true
steps:
  - create:
      path: "{{featurePath}}/{{ name | snake_case }}.dart"
      template: |
        class {{ name | pascal_case }} {}
      ifExists: fail
      format: false
