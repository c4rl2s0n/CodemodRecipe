name: composed_recipe_example
description: Example showing how to compose multiple recipes with shared arguments
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  codemod_recipe:
    path: ../..

dev_dependencies:
  lints: ^6.1.0

executables:
  composed_codemod: composed_codemod
