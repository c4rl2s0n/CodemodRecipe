name: add_method_example
description: Example showing how to add a method to an existing class
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  codemod_recipe:
    path: ../..

dev_dependencies:
  lints: ^6.1.0

executables:
  add_method: add_method
