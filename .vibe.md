# Project Context: codemod_recipe

> **Purpose**: Shared context for agents working on this project. Read this first for structure, conventions, and tooling.

## Agent tooling

### Code intelligence (first step)

For structural questions — where is X, who calls Y, architecture, impact — **use codebase-memory-mcp before grep or manual reads.**

- Playbook: `.cursor/skills/codebase-memory/reference.md`
- MCP setup: https://github.com/DeusData/codebase-memory-mcp

### Structural edits (codemod MCP)

Deterministic multi-language edits via YAML recipes — preview then apply:

- Playbook: `.cursor/skills/codemod-mcp/reference.md`
- Human doc: `docs/codemod-mcp.md`
- Rust MCP: `cargo run -q --manifest-path rust/Cargo.toml -p codemod_recipe_host --bin codemod_mcp -- --workspace-root . --codemod-root .codemod`

Pair **codebase-memory** (locate → impact) with **codemod-mcp** (`preview_recipe` → `apply_recipe`).

## Project overview

**Name**: codemod_recipe  
**Purpose**: YAML recipe-driven codemods with a Rust tree-sitter engine, stdio host, MCP server, and VS Code extension  
**Primary languages**: Rust (runtime), TypeScript/Vue (extension)

## Layout

| Path | Role |
|------|------|
| `rust/` | Engine, YAML model, `codemod_host`, `codemod_mcp` |
| `vscode_extension/` | VS Code UI + bundled host binary |
| `.codemod/` | Shipped recipes, maps, templates |
| `test/fixtures/` | Rust integration/golden fixtures |
| `export/` | Bootstrap skills/rules copied into consumer projects |

## Recipes

Recipes require `id` and `steps`. Agent skill: `codemod-yaml-dsl` (under `export/.agents/skills/`).

## Tests

```bash
cd rust && cargo test --all
cd vscode_extension/src/webview && npm test
```

See `CONTRIBUTING.md` and `ARCHITECTURE.md` for detail.

---

## 🗺️ Resource Discovery

> **Single source of truth**: This section maps ALL project resources. Agents should consult this first — no need to duplicate content elsewhere.

### 🎯 Quick Start Guide

| Resource Type | Primary Location | When to Use |
|---------------|------------------|-------------|
| **Core Skills** | `.vibe/skills/` | Always available to Vibe agents |
| **Project Skills** | `.cursor/skills/` | Cursor-specific workflows and playbooks |
| **Agent Skills** | `.agents/skills/` | Specialized subagent capabilities |
| **Main Docs** | `docs/` | Human-readable guides and references |
| **Rules & Policies** | `.cursor/rules/` | Cursor rule configurations |
| **Agent Configs** | `.vibe/agents/` | Vibe subagent definitions |
| **Exportable** | `export/` | Resources for distribution to other projects |

### 📚 Skills Catalog

#### Core Vibe Skills (`/.vibe/skills/`)
Primary skills loaded by Mistral Vibe agents in this repository:

| Skill | Purpose | Documentation |
|-------|---------|---------------|
| `agent-coordinator` | Coordinate between specialized subagents | `SKILL.md` |
| `codebase-memory` | Code intelligence via MCP | `SKILL.md`, `reference.md` |
| `codemod-mcp` | Codemod MCP server operations | `SKILL.md` |
| `yaml-dsl` | YAML DSL for codemod recipes | `SKILL.md` |

#### Cursor Skills (`/.cursor/skills/`)
Project-specific skills for Cursor integration:

| Skill | Purpose | Documentation |
|-------|---------|---------------|
| `codebase-memory` | Codebase memory integration | `SKILL.md`, `reference.md` |
| `codemod-mcp` | Codemod MCP workflows | `SKILL.md`, `reference.md` |
| `codemod-recipe-change-checklist` | Change management checklist | `SKILL.md` |
| `codemod-recipe-design-patterns` | Recipe design patterns | `SKILL.md` |
| `codemod-recipe-human-docs` | Human documentation generation | `SKILL.md` |
| `codemod-recipe-repo-orientation` | Repository orientation | `SKILL.md` |
| `codemod-recipe-vscode-extension-backend` | VS Code extension backend | `SKILL.md` |
| `codemod-recipe-vue-development` | Vue development workflows | `SKILL.md` |
| `codemod-recipe-webview-extension-contract` | Webview extension contract | `SKILL.md` |
| `codemod-recipe-webview-ui-development` | Webview UI development | `SKILL.md` |
| `code-quality` | Code quality standards | `SKILL.md`, `reference.md` |

#### Agent Skills (`/.agents/skills/`)
Specialized skills for subagent operations:

| Skill | Purpose | Documentation |
|-------|---------|---------------|
| `codemod-languages` | Language support for codemods | `SKILL.md` |
| `codemod-mcp-playbook` | MCP server playbook | `SKILL.md` |
| `codemod-overview` | Codemod system overview | `SKILL.md` |
| `codemod-recipe-authoring` | Recipe authoring guide | `SKILL.md` |
| `codemod-recipe-design-patterns` | Design patterns for recipes | `SKILL.md` |
| `codemod-tree-sitter-queries` | Tree-sitter query patterns | `SKILL.md` |
| `codemod-yaml-dsl` | YAML DSL reference | `SKILL.md` |
| `recipe-generation` | Automatic recipe generation | `SKILL.md` |

#### Legacy Skills (`/skills/`)
Historical/legacy skills (maintained for compatibility):

| Skill | Purpose | Documentation |
|-------|---------|---------------|
| `code_quality` | Legacy code quality | `SKILL.md` |

### 📖 Documentation Hub

#### Main Documentation (`/docs/`)
Comprehensive guides and references:

- **Getting Started**: `getting-started.md` - Complete onboarding guide
- **Codemod MCP**: `codemod-mcp.md` - MCP server documentation
- **Writing Recipes**: `writing-recipes.md` - Recipe authoring guide
- **Recipe Templates**: `recipe-templates.md` - Pre-built recipe patterns
- **Recipe Shortcuts**: `recipe-shortcuts.md` - Common recipe patterns
- **Tree-sitter Queries**: `tree-sitter-queries.md` - Query language reference
- **Recipe Design Patterns**: `recipe-design-patterns.md` - Best practices
- **Language Support**: `language-support.md` - Supported languages
- **New Project Rust MCP**: `new-project-rust-mcp.md` - Rust MCP setup guide

#### Skill Documentation
Each skill directory contains its own documentation:
- **SKILL.md**: Primary skill documentation with usage instructions
- **reference.md**: Detailed reference material (where available)

### ⚖️ Rules & Policies

#### Cursor Rules (`/.cursor/rules/`)
Model context files for Cursor integration:

- `codebase-memory.mdc` - Codebase memory configuration
- `codemod-recipe.mdc` - Codemod recipe rules

#### Rulesets (`/export/rulesets/`)
Pre-configured ruleset bundles:

- **Recommend**: `export/rulesets/recommend/.cursor/rules/` - Recommended rules
- **Strict**: `export/rulesets/strict/.cursor/rules/` - Strict enforcement rules

### 🤖 Agent Configurations

#### Vibe Agents (`/.vibe/agents/`)
Subagent configurations in TOML format:

- `code-reviewer.toml` - Code review specialist
- `codestral-coder.toml` - Primary coding agent
- `doc-writer.toml` - Documentation specialist
- `test-writer.toml` - Test generation specialist

#### Agent Skills Directory (`/.agents/skills/`)
See [Agent Skills](#-agent-skills) section above.

### 📦 Export Directory (`/export/`)

Resources designed for distribution to consumer projects:

| Path | Purpose | Contents |
|------|---------|----------|
| `export/rulesets/` | Pre-configured ruleset bundles | Recommend, Strict variants |
| `export/companions/` | Companion MCP configurations | Codebase-memory companion |
| `export/.agents/skills/` | Exportable agent skills | 7 core skills for distribution |

**Exportable Agent Skills** (`/export/.agents/skills/`):
- `codemod-yaml-dsl`, `recipe-generation`, `codemod-languages`, `codemod-recipe-design-patterns`, `codemod-mcp-playbook`, `codemod-tree-sitter-queries`, `codemod-overview`

### 🔍 Resource Lookup Guide

**Need to find something?** Use this decision tree:

1. **Looking for agent capabilities?** → Check [Skills Catalog](#-skills-catalog)
2. **Need human-readable docs?** → Check [Documentation Hub](#-documentation-hub)
3. **Setting up rules/policies?** → Check [Rules & Policies](#-rules--policies)
4. **Configuring subagents?** → Check [Agent Configurations](#-agent-configurations)
5. **Distributing to other projects?** → Check [Export Directory](#-export-directory)

### 🔗 Cross-Reference Index

**Related Resources:**
- `codebase-memory`: Skills in `.vibe/skills/`, `.cursor/skills/`, export companion in `export/companions/`
- `codemod-mcp`: Skills in `.vibe/skills/`, `.cursor/skills/`, docs in `docs/codemod-mcp.md`
- `yaml-dsl`: Skills in `.vibe/skills/`, `.agents/skills/`, export in `export/.agents/skills/`
- `design-patterns`: Skills in `.cursor/skills/`, `.agents/skills/`, docs in `docs/recipe-design-patterns.md`
