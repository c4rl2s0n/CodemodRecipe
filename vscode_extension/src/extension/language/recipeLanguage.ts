/**
 * Recipe YAML language support — see {@link LanguageSession}.
 * @deprecated Import from `./session` for new code.
 */
export {
  LanguageSession,
  registerRecipeLanguageSupport,
} from './session';

export { collectSetWithKeys } from './recipeHelpers';
