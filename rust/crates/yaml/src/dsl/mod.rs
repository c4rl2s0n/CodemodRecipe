//! Schema-aligned YAML DSL wire constants (`recipe.schema.json`, `map.schema.json`, `variables.schema.json`).
//!
//! Author-facing descriptions and VS Code hovers come from [`crate::dsl_vocabulary::ENTRIES`], not from doc comments on individual constants here.

pub mod map_asset;
pub mod recipe;
pub mod variables_asset;
